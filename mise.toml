[tools]
uv = "0.12.19"
rust = "1.97.1"
go = "1.27.1"
"cargo:cargo-binstall" = "1.23.0"
"cargo:zizmor" = "1.30.1"
# Homebrew's mise 2026.9.1 still resolves the pre-transfer jdx/aube registry
# entry, so use the current GitHub repository explicitly until that registry updates.
"github:aubepkg/aube" = "latest"
